# 2026-09-11 ~ 09-16 일자별 기록

09-10 발행 이후 운영 중 발견·수정한 항목을 근거와 함께 정리합니다. 전부 AGENTS.md(비공개 운영 로그)에 시각·수치와 함께 원본 기록이 있으며, 여기서는 공개 가능한 요약만 남깁니다. 기존 공식 모델·번들 수치는 변경하지 않았습니다.

## 1. GRID/ASOS 403(할당량 소진) 반복의 근본원인 발견·수정(09-15)

주 초 내내 반복되던 "구키 하루 할당량 소진 → 403" 현상의 실제 원인은 외부 API 쿼터 부족이 아니라 GRID 수집기의 **중복 호출**이었습니다. 하루 호출량이 한도(20,000회)의 92%(18,432회)까지 도달했던 것을 중복 제거로 약 97% 감축했습니다. 이후 자정 리셋마다 재확인한 결과 경계 시각(00:00~00:12 사이) 1건을 제외하면 403이 재발하지 않았습니다(09-13~09-16 4일 연속 확인).

## 2. 4지역 공식 성능지표 용량은 "발전소 API 정격(AC)"으로 통일(09-16)

인버터 등록용량 합계(예: 김제 1,100kW)와 실제 발전소 API 정격(김제 999.005kW)이 서로 다른 값이었고, 관측 최대 AC 출력은 4지역 모두 API 정격보다 낮았습니다. nMAE 분모와 대시보드 공식용량은 API 정격 기준으로 통일했습니다(부안 998.715 · 김제 999.005 · 영광 639.94 · 광주 240.0kW).

광주는 인버터 5대 등록용량 합계가 241.58kW로 API 정격과 달라, 역할을 분리했습니다. 수집기 구성 검증과 예측의 물리 clip 상한은 241.58kW, 공식 대시보드 용량과 nMAE 분모는 240.0kW입니다. 인버터 구성 이상 감시(Blockdata watchdog)는 등록용량 합계 기준을 유지합니다(구성 변경 감지 목적).

## 3. 광주 "단기(+1h/+24h/+48h)" Shadow 관측 - 3주간 무성공 버그 발견·수정(09-16)

08-26 생성 이후 약 3주간 이 tier의 라이브 실행이 단 한 번도 `success`를 낸 적이 없었습니다(실측: error 52건 + warming_up 332건 + success 0건). 원인은 광주 ASOS(station 156) 관측치가 DB에 도착하는 데 실제로 상당하고 편차가 큰 지연이 있다는 것이었습니다(9월 전체 측정: 중앙값 72.6분, p90 372.6분, p95 552.6분, 120분 초과 23%). 매시 정각 실행이 "이번 정시" 값을 요구했기 때문에 그 값은 구조적으로 존재할 수 없었습니다.

1차로 시도한 고정 2시간 오프셋은 위 분포의 긴 꼬리 때문에 재발 방지로 부족함이 재검증으로 드러났습니다(약 20%는 여전히 실패). 최종적으로 고정 오프셋 대신 ASOS 핵심 관측(기온·습도·운량)이 실제로 채워진 가장 최근 시각을 매번 최대 12시간까지 동적으로 탐색하도록 바꿨습니다. 못 찾으면 정직하게 "대기"로 처리합니다(가짜 성공 없음). 실제 예약작업 재실행으로 `success` 결과가 나오는 것을 처음으로 확인했습니다.

+48h 리드는 GRID 수집기가 당일 8개 고정 시각만 수집하는 구조적 한계로 여전히 데이터 자체가 없을 수 있습니다(위 시간지연 문제와는 별개의 원인).

## 4. 4지역 "+48h pooled 후보 Shadow" 라이브 신규 구축(09-16)

기존 "+24h pooled 후보 Shadow"(09-10, GRID/정규 NWP 기반, 4지역)에 이어 +48h(37~59h 리드)용 라이브 조립기를 신규 제작했습니다. +48h는 GRID가 도달하지 못하는 리드타임이라 KIM NC(D+1/D+2) 자료를 직접 사용합니다. 부안·김제·영광은 기존 +48h 후보 모델(09-10 제작)을, 광주는 자체 +48h 후보 모델(09-11 제작, XGBoost, MAE 7.5kW - 표본 소수로 공식 승격 보류 상태 유지)을 그대로 사용합니다.

구축 과정에서 `asos_hourly` 테이블의 `cloud_pct`(0~100)와 `cloud_tenths`(0~10, =pct/10) 두 컬럼을 09-10 코드가 혼동해 전운량 특성에 10배 스케일 오차가 섞여 들어가고 있던 것을 발견해 즉시 수정했습니다. 09-10~09-16 사이 저장된 예측 중 이 특성을 사용한 것은 스케일이 잘못된 입력 기반이므로 신뢰할 수 없습니다(과거 기록은 감사 목적으로 보존, 소급 정정하지 않음). 광주는 이 테이블을 쓰지 않는 별도 스키마라 영향받지 않았습니다.

## 5. 김제 가을 D+1 완전표본 0일 → 4일 - 인버터별 비율보정(09-16)

김제 라이브 브릿지의 전수 게이트(인버터 10대 중 1대라도 결측이면 나머지 9대의 정상 실측값까지 전부 NaN 처리)가 부분관측을 이미 지우고 있던 버그를 발견해 수정했습니다(그룹 게이트 제거, 인버터 개별 값 존재 여부만으로 판정). 이어 김제 전용으로 인버터별 상대강도 비율보정(원본 값이 있으면 항상 원본 우선, 결측 시에만 대체)을 구현해 가을 완전표본을 0일에서 4일로 늘렸습니다. 영광 데이터로 수정 전/후 daily_energy_kwh 값을 비교해 회귀가 없음을 확인했습니다(최대 차이 0.019kWh, 부동소수 반올림 수준). 부안·영광은 이 보정 없이도 표본을 채울 수 있는 구조라 확대 적용하지 않았습니다.

## 6. KIM NC D1D2(+24h/+48h) 백필 진행 상황

4지역 모두 2024-08-25 이후 아카이브를 역방향으로 채우는 백필이 진행 중입니다. 초기 지역별 배분(광주 우선)은 09-15 API 키 2개 병행 체계로 전환하며 4지역 균등 배분으로 대체됐습니다. 09-16 기준 실측으로 부안·김제·영광은 완주까지 약 5.0~5.1일, 광주는 1일 늦게 시작한데다 leg별(최근구간/과거구간) 예산 구조 특성상 격차가 좁혀지지 않아 약 6.1일로 추정됩니다(운영 판단 사안, 코드 조치 없음).

## 7. 광주 초단기 입력 정렬 최소 완화와 재검증(09-16)

광주 Blockdata 벤더의 낮시간 갱신 지연은 수집기 버그가 아니라 원천 응답이 직전 측정값을 재반환하는 외부 소스 특성으로 확인됐습니다. 가짜 데이터를 보간하지 않고, 발전량 lag 15·30·60분의 입력 정렬 허용오차만 광주에 한해 ±4분에서 명목 수집주기 한 칸인 ±5분으로 제한 조정했습니다. lag 0분은 ±1분, 다른 3지역은 ±4분을 유지합니다.

최종 입력·용량 정책이 반영된 09-16 17:50부터 7일 운영 신뢰성 게이트를 새로 시작했습니다. 야간 목표시간은 학습 도메인 밖이므로 정상 대기로 기록하며, 결측을 성공으로 바꾸지 않습니다.

## 요약

이번 구간 수정은 전부 **버그 발견·수정** 또는 **관측 인프라 확장**이며, 기존 공식 모델의 수치나 운영 스케줄을 바꾸지 않았습니다. 공식 승격 판정은 이전과 동일하게 보류 상태입니다.
